import AppLayout from "@/src/components/AppLayout";
import ForumCreate from "@/src/feat-components/ForumCreate";

const ForumCreatePage = () => {
  return <AppLayout children={<ForumCreate />} />;
};

export default ForumCreatePage;
